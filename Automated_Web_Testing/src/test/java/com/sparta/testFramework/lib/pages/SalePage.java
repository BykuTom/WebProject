package com.sparta.testFramework.lib.pages;

import org.openqa.selenium.WebDriver;

public class SalePage extends WebPage{
    public SalePage(WebDriver webDriver) {
        super(webDriver);
    }
}
